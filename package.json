{
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm run bs --prefix server",
    "client": "npm run dev --prefix client"
    },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
